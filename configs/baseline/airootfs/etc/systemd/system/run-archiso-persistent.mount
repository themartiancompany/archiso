[Unit]
Description=Persistent mount
RequiresMountFor=/etc/keys

[Mount]
What=/dev/mapper/persistent
Where=/run/archiso/persistent
Type=ext4
Options=rw,noatime,nofail
TimeoutSec=5

[Install]
WantedBy=multi-user.target
